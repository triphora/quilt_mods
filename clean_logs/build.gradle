import com.emmacypress.gradle.QuiltModJson

version = '1.2.0'

outlet {
	mcVersionRange = '>=1.18.2'
	allowSnapshotsForProject = false
}

emmod {
	mixin = false
	summary = 'Filters out messages in the console/log by user-defined filters.'
	spdx = 'LGPL-3.0-or-later'
	modrinthSlug = 'clean-logs'
	javaVersion = 15
	gameVersions = outlet.mcVersions()
	depends = [new QuiltModJson.QuiltLoader.Dependency(
		'quilt_loader', '>=0.17.0', 'Quilt Config is in use by this project'
	)]
	contributors = ['Bravarly': 'Former Author']
	entrypoints = [
		"pre_launch": "com.emmacypress.clean_logs.CleanLogs",
	]
}
