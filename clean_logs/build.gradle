tasks.withType(JavaCompile).configureEach {
  it.options.release = 15
	it.sourceCompatibility = 15
}

loom.runtimeOnlyLog4j = false

modrinth {
  projectId = 'clean-logs'
  gameVersions.add('1.18.2')
}
